#intro.left
	%p Look up word associations for inspiration and ideas.
	%p A great tool for:
	%ul
		%li Journalists
		%li Lyricists
		%li Crossword Players
		%li and others…

%img{:class => "divider", :src => "../assets/images/divider.png", :alt => "divider.png"}

#home_search.left
	%h1 Find words associated with:
	%form{:id => 'live_search', :method => 'get', :action => words_search_path}
		%input{:id => 'search_input', :name => 'subject', :type => 'text'}
		%input{:name => 'syllables', :value => '', :type => 'hidden'}
		%button{:type => 'submit', :value => 'Go'}Go
	#home_search_results
